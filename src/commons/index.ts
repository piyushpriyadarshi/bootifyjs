/**
 * BootifyJS framework commons — generic, dependency-free primitives shared
 * across framework modules and exported for application use.
 *
 * Placement rule: a utility belongs here when it is generic, framework-
 * agnostic, and used by at least two framework modules. ("Framework commons"
 * — distinct from `@priyadarship4/commons`, the cross-repo building blocks.)
 */
export * from './single-flight'
export * from './stable-stringify'
export * from './path-match'
